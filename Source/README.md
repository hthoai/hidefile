# Operating Systems
## Project 1: A simple Linux char driver to hide file
### How to:
* Run:
`make all`
* Load driver:
`make load`
* Hide a file:
`make hide` (then enter *file_path*)
* Unload driver:
`make unload`
* Clean:
`make clean`